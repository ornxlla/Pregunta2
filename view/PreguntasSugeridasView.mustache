<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Preguntas Sugeridas por Usuarios</title>
    <link rel="stylesheet" href="/public/css/login.css">
</head>
<body>

<div class="page-container">
    <h1 class="page-title">Preguntas Sugeridas por Usuarios</h1>

    <div class="container">
        <div class="lobby-container">

            <table id="sugerencia-tabla" class="responsive-table">
                <thead>
                <tr>
                    <th>ID Pregunta</th>
                    <th>Pregunta</th>
                    <th>Respuestas</th>
                    <th>Acciones</th>
                </tr>
                </thead>
                <tbody>
                {{#preguntas_sugeridas}}
                    <tr>
                        <td>{{id_pregunta}}</td>
                        <td>{{pregunta}}</td>
                        <td>
                            <ul>
                                {{#respuestas}}
                                    <li>{{respuesta}}</li>
                                {{/respuestas}}
                                {{^respuestas}}
                                    <li>No hay respuestas</li>
                                {{/respuestas}}
                            </ul>
                        </td>
                        <td>
                            <form action="/Pregunta/aprobarPreguntaSugerida" method="POST">
                                <input type="hidden" name="id_pregunta" value="{{id_pregunta}}">
                                <button type="submit" class="action-button">Aprobar</button>
                            </form>
                        </td>
                    </tr>
                {{/preguntas_sugeridas}}
                </tbody>
            </table>
        </div>
    </div>

    <div class="links-container">
        <a href="/Pregunta/listadoGeneralPreguntas" class="page-link">Listado de Preguntas General</a>
        <a href="/Pregunta" class="page-link">Panel de Control</a>
    </div>
</div>

</body>
</html>
