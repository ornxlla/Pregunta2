<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Preguntas Reportadas</title>
    <link rel="stylesheet" type="text/css" href="/public/css/login.css">
</head>
<body>

<div class="page-container">
    <div class="table-container">
        <h1 class="page-title">Preguntas Reportadas</h1>
        <table class="question-table">
            <thead>
            <tr>
                <th class="table-header">ID</th>
                <th class="table-header">Pregunta</th>
                <th class="table-header">Acciones</th>
            </tr>
            </thead>
            <tbody>
            {{#preguntas_reportadas}}
                <tr>
                    <td>{{id_pregunta}}</td>
                    <td>{{texto}}</td>
                    <td>
                        <form action="/Pregunta/aprobarPregunta" method="POST" class="action-form">
                            <input type="hidden" name="id_pregunta" value="{{id_pregunta}}">
                            <button type="submit" class="action-button">Borrar Reporte</button>
                        </form>
                        <form action="/Pregunta/darDeBajaPregunta" method="POST" class="action-form">
                            <input type="hidden" name="id_pregunta" value="{{id_pregunta}}">
                            <button type="submit" class="action-button button-danger">Borrar pregunta</button>
                        </form>
                    </td>
                </tr>
            {{/preguntas_reportadas}}
            </tbody>
        </table>
        <br>
        <a href="/Pregunta/listadoGeneralPreguntas" class="page-link">Listado de Preguntas General</a>
        <a href="/Pregunta" class="page-link">Panel de Control</a>
    </div>
</div>

</body>
</html>
