<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Preguntas Reportadas</title>
    <link rel="stylesheet" type="text/css" href="/public/css/styles.css">
</head>
<body>
<div class="container">
    <div class="table-container">
        <h1>Preguntas Reportadas</h1>
        <table>
            <thead>
            <tr>
                <th>ID</th>
                <th>Pregunta</th>
                <th>Acciones</th>
            </tr>
            </thead>
            <tbody>
            {{#preguntas_reportadas}}
                <tr>
                    <td>{{id_pregunta}}</td>
                    <td>{{texto}}</td>
                    <td>
                        <form action="/Pregunta/aprobarPregunta" method="POST" style="display: inline;">
                            <input type="hidden" name="id_pregunta" value="{{id_pregunta}}">
                            <button type="submit">Borrar Reporte</button>
                        </form>
                        <form action="/Pregunta/darDeBajaPregunta" method="POST" style="display: inline;">
                            <input type="hidden" name="id_pregunta" value="{{id_pregunta}}">
                            <button type="submit" class="button-danger">Borrar pregunta</button>
                        </form>
                    </td>
                </tr>
            {{/preguntas_reportadas}}
            </tbody>
        </table>
        <br>
        <a href="/Pregunta/listadoGeneralPreguntas">Listado de Preguntas General</a>
        <a href="/Pregunta/getPanel" class="panel-link">Panel de Control</a>

    </div>
</div>
</body>
</html>
